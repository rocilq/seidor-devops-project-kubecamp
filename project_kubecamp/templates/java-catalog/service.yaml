apiVersion: v1
kind: Service
metadata:
  name: {{ include "project_kubecamp.fullname" . }}
  labels:
    {{- include "project_kubecamp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "project_kubecamp.selectorLabels" . | nindent 4 }}
